version: "3.9"

services:
  backend:
    build:
      context: ./backend
    container_name: gestor-backend
    ports:
      - "3004:3004"
    env_file:
      - ./backend/.env
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
    container_name: gestor-frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
    restart: unless-stopped
